import 'package:flutter/material.dart';

void main() => runApp(MaterialApp(
      debugShowCheckedModeBanner: false,
      title: 'Navigation In flutter',
      theme: ThemeData(
          accentColor: Colors.blueAccent, brightness: Brightness.light),
    ));
