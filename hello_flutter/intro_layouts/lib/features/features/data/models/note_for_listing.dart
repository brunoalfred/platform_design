class NoteForListing {
  String noteId;
  String noteTitle;
  DateTime createDataTime;
  DateTime lastEditDateTime;

  NoteForListing(
      {this.noteId,
      this.noteTitle,
      this.createDataTime,
      this.lastEditDateTime});
}
